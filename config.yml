plugins:
# Disrupting host time
- timeburglar:
   shift: 100
   startup: hot
   frequency: fixed
   interval: PT01S
# Dropping client connection
- dropper:
    target: client
    ip: backend
    unavailable_seconds: 60
    frequency: fixed
    interval: PT01S
# Dropping backend container
- deathloop-sh:
    target: backend
    frequency: random
    min_interval: PT10M
    max_interval: PT20M
